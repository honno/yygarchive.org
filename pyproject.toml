[project]
name = "yygarchive.org"
version = "0.1.0"
requires-python = ">=3.13.3"

dependencies = [
  "flask",
  "peewee",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"